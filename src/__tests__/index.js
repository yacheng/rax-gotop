import {createElement, Component} from 'rax';
import renderer from 'rax-test-renderer';
import Gotop from '../';

describe('Gotop', () => {
  it('test typeof Gotop', () => {
    expect(typeof Gotop).toEqual('function');
  });
});
